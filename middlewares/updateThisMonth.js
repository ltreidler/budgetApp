// const mongoose = require('mongoose');
// require('../models/User');
// require('../models/')

// const User = mongoose.model('User');

// module.exports = async ({user: {_id}}, res, next) => {
//     //update the mongoose user based on which items are from this month
//     const {moneyID: money} = await User.findById(_id)
//                                 .populate('moneyID');

// }
